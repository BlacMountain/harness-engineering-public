# Product Specs

This repository's product is a reusable harness guidance system.

Current goals:

- Make Agent startup rules discoverable through `AGENTS.md`.
- Make durable constraints machine-readable through `.harness/*.yaml`.
- Make Git governance explicit through `docs/GIT_POLICY.md`.
- Make policy validation executable through `scripts/harness-check`.
