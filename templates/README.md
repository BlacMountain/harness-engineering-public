# Harness Templates

Copy one `templates/<project-type>/` directory into a target project when the
target has no local harness yet. After copying, edit project name, workspace
type, repository paths, commands, artifact store, and forbidden paths.

Templates are bootstrap material. The target project's copied files become the
long-term authority.
