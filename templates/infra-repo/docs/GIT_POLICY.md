# Git Policy

Version infrastructure code, deployment templates, and runbooks. Do not version
secrets, local env files, credential material, Terraform state, or generated
plans containing sensitive values.
