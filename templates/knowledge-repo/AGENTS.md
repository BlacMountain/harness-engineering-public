# AGENTS.md

This is a knowledge repository. Before work:

1. Read `.harness/*.yaml`.
2. Confirm repository root and run `git status --short`.
3. Keep durable rules in policy, docs, scripts, lint, tests, or CI.

Knowledge repositories do not require a virtual environment unless code or
validation tooling needs one.
