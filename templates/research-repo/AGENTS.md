# AGENTS.md

This is a research repository. Before work:

1. Read `.harness/*.yaml`.
2. Confirm repository root and run `git status --short`.
3. Use an isolated dependency environment.
4. Keep code, configs, docs, and lightweight fixtures in git.

NEVER commit datasets, generated results, checkpoints, trained models, large
artifacts, secrets, or local environment files.
